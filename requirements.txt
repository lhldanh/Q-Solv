# --- Core ML / HF stack ---
transformers>=4.45.0
datasets>=2.10.0
tokenizers>=0.19.0
peft>=0.10.0
trl>=0.8.0
accelerate>=0.30.0
safetensors>=0.4.0
huggingface-hub>=0.23.0
sentencepiece>=0.1.98
bitsandbytes>=0.43.0

# --- Training & Optimization ---
unsloth>=2024.4
xformers

# --- Backend (The Engine) ---
fastapi>=0.110.0
uvicorn>=0.27.0
llama-cpp-python>=0.2.76
python-dotenv>=1.0.0

# --- Frontend (The IDE) ---
streamlit>=1.35.0
streamlit-ace>=0.1.10

# --- Utilities & Data ---
tqdm>=4.66.0
pandas>=2.0.0
requests>=2.31.0
pyyaml>=6.0.1
matplotlib>=3.8.0
seaborn>=0.13.0

# NOTE: PyTorch (torch, torchvision, torchaudio) MUST be installed separately 
# via pytorch.org to match your CUDA version.